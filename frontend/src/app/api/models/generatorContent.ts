import { generatorsContent } from "./generatorsContent"

export const generatorContent = "generator {baseOutput isBroken pollution}"
export const generatorQuery = `query generator {${generatorsContent}}`